{
  "name": "@aerorf/shared",
  "version": "1.0.0",
  "description": "Tipos e contratos compartilhados AeroRF",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": "github:AeroRF/aerorf-packages",
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
